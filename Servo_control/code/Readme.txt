############ check these steps before uploading code (servo_control.py) to Raspberry pi ############

1 -> The code uses pigpio library for servo control.Make sure your RPi has pigpio installed.
2 -> after installation run "$sudo pigpiod" in your cmd.
